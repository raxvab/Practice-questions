#include <bits/stdc++.h>
typedef long long int lli;
using namespace std;

int main() {
lli n,i,j,c=0;
    cin>>n;
    string s;
    for( i=0;;i++)
    {
        s=s+"47";
 sort(s.begin(),s.end());
        
     
       for(j=0;;j++)
           { 
               if(stoll(s)>=n)
               {
                cout<<s<<endl;
                c++;
                break;
               
            }
            if(next_permutation(s.begin(),s.end()))
          
            
            break;        
           	
           }
        if(c>0)
         break;
    }
	return 0;
}
